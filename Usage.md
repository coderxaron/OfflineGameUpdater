Require on __Server__ !

Example : 

'''
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local GameUpdater = ReplicatedStorage:WaitForChild("GameUpdater")

local RS = GameUpdater.init(RS)

'''